# Python packages — install with: pip install -r requirements.txt

# --- Geospatial ---
geopandas>=1.0
pyogrio>=0.7          # GeoPackage / vector I/O engine used by geopandas
shapely>=2.0
pyproj>=3.5
pyrosm>=0.6           # reads the clipped OSM PBF in notebook 02

# --- Data ---
pandas>=2.0
numpy>=1.24
pyarrow>=14.0         # parquet engine — notebook 09 writes and 10 reads the ground truth
openpyxl>=3.1         # reads the colour-coded annotation workbook in notebook 09

# --- Notebooks ---
jupyterlab>=4.0

# --- Tests ---
pytest>=7.4

# NOTE — system dependency for notebook 02 (OSM PBF clipping):
# Notebook 02 calls `osmium extract` via subprocess — install the osmium-tool
# CLI separately (this is a system package, not a pip package):
#   Linux:  sudo apt install osmium-tool
#   Mac:    brew install osmium-tool
#   Windows/conda: conda install -c conda-forge osmium-tool
