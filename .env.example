# Copy to .env (git-ignored) and paste the TU Braunschweig KI-Toolbox token.
# lib/llm_client.read_token reads this file before every call, so a renewed
# token takes effect without restarting the run.
TU_KI_TOOLBOX_TOKEN=
